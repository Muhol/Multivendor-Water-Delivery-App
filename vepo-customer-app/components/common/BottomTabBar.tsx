import { View, Text } from 'react-native'
import React from 'react'

type Props = {}

const BottomTabBar = (props: Props) => {
  return (
    <View className='w-full h-[50px] bg-white'>
      <Text>BottomTabBar</Text>
    </View>
  )
}

export default BottomTabBar